import tw from "twin.macro";
import styled from "@emotion/styled";

import Icon from "../../component/Icon";

const DashboardHeadlineIcon = styled(Icon)([
    tw`mr-2 md:mr-3 text-4xl md:text-6xl`,
]);

export default DashboardHeadlineIcon;
