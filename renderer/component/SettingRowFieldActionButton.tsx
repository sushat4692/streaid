import tw from "twin.macro";
import styled from "@emotion/styled";

import Button from "../../component/Button";

const SettingRowFieldAction = styled(Button)(tw`mr-4`);

export default SettingRowFieldAction;
